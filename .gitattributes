# Auto detect text files and perform LF normalization
* text=auto

# Ensure shell scripts always use LF line endings (for cross-platform compatibility)
*.sh text eol=lf

# PowerShell scripts use CRLF
*.ps1 text eol=crlf
